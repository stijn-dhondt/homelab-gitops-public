apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cluster-backup-data
  namespace: cluster-backup
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
