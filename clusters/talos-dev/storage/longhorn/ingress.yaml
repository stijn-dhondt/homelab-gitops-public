apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-ui
  namespace: longhorn-system
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    # Forward-auth via Authentik's shared domain-level provider - see
    # clusters/talos-dev/apps/authentik/README.md ("How apps get protected").
    nginx.ingress.kubernetes.io/auth-url: "http://authentik-server.authentik.svc.cluster.local/outpost.goauthentik.io/auth/nginx"
    nginx.ingress.kubernetes.io/auth-signin: "https://longhorn.lab.example.com/outpost.goauthentik.io/start?rd=$scheme://$http_host$escaped_request_uri"
    nginx.ingress.kubernetes.io/auth-response-headers: "Set-Cookie,X-authentik-username,X-authentik-groups,X-authentik-entitlements,X-authentik-email,X-authentik-name,X-authentik-uid"
    nginx.ingress.kubernetes.io/auth-proxy-set-headers: "longhorn-system/authentik-auth-headers"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - longhorn.lab.example.com
      secretName: longhorn-ui-tls
  rules:
    - host: longhorn.lab.example.com
      http:
        paths:
          # forward_single mode's OAuth-style callback happens on the app's own host
          # (longhorn.lab.example.com), not authentik.lab.example.com - this path has to
          # reach Authentik's outpost, not Longhorn itself, or the callback 404s.
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: authentik-outpost
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: longhorn-frontend
                port:
                  number: 80